const Loader = () => {
  return (
    <div className="fixed inset-0 flex items-center justify-center z-50">
      <div className="size-16 border-4 border-t-4 border-gray-200 border-t-slate-900 rounded-full animate-spin"></div>
    </div>
  );
};

export default Loader;
